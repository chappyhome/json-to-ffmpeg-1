name = "json-to-ffmpeg-worker"
main = "dist/index.js"
compatibility_date = "2024-06-01"
compatibility_flags = ["nodejs_compat"]

[build]
command = "npm --prefix .. run build && npm install && npm run build"
